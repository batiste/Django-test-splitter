Django-test-splitter
====================

Django test splitter is a test runner that splits your tests and execute them in parallel in different processes.

The goal is to speed up the execution of long test suites.